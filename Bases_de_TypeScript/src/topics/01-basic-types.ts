
let name:string = "Strider";
let hpPoints: number | string | boolean | "FULL"= 95; //HP Points toma dos tipos de valores: number, string, boolean y "FULL".
const isAlive: boolean = true

hpPoints = "FULL";

console.log({
    name,hpPoints,isAlive
})
export {};